# LangChain Learning

LangChain 学习项目，基于 Python 3.12 + uv 管理。

## 项目结构

- `chapter_*/`: 各章节学习笔记与代码（Jupyter notebook + Python 模块），章节持续增加
- `main.py`: 入口文件

## 技术栈

- 包管理: uv（uv.lock）
- 依赖: 以 `pyproject.toml` 为准
- Python: >=3.12

## 约定

- 使用约定式提交（Conventional Commits）
- API 密钥通过 `.env` 管理，不提交到仓库
